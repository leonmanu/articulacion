<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('../../partials/header') %> 
</head>
<body>
    <%- include('../../partials/spinner') %> 
    <%- include('../../partials/navbar') %> 
    
    <!-- Navber secundario -->
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <div class="container">
            <div class="navbar-brand" href="#">Estudiantes</div>
            <button type="button" class="btn btn-info" id="btn-editar">Editar</button>
            <button type="button" class="btn btn-success d-none" id="btn-guardar">Guardar</button>
        </div>
    </nav>

    <!-- Contenido Principal -->
    <div class="container mt-4">
        <table class="table table-striped table-hover">
            <thead class="table-primary text-center">
                <tr>
                    <th class="d-none" > Change </th>
                    <th class="d-none" >RN</th>
                    <th>ID</th>
                    <th>Escuela</th>
                    <th>Curso</th>
                    <th>Fecha Inscripción</th>
                    <th>Estado Inscripción</th>
                    <th>Estudiante</th>
                    <th>Documento</th>
                    <th>Escuela Destino</th>
                </tr>
            </thead>
            <tbody>
                <% estudiantes.forEach(estudiante => { %>
                    <tr>
                        <td class="change d-none">false</td>
                        <td class="text-center d-none"><%= estudiante.rowNumber %></td>
                        <td class="text-center"><%= estudiante.id %></td>
                        <td class="text-center"><%= estudiante.claveEscuela %></td>
                        <td class="text-center"><%= estudiante.claveCurso %></td>
                        <td class="text-center"><%= estudiante['Fecha inscripción'] %></td>
                        <td class="text-center"><%= estudiante['Estado inscripción'] %></td>
                        <td class="text-center estudiante"><%= estudiante.estudiante %></td>
                        <td class="text-center"><%= estudiante.documento %></td>
                        <td>
                            <input class="form-control" list="datalistOptions" id="exampleDataList" placeholder="Buscar..." disabled>
                            <datalist id="datalistOptions">
                                <% claveEscuelas.forEach(escuela => { %>
                                    <option value="<%= escuela %>"><%= escuela %></option>
                                <% }); %>
                            </datalist>
                        </td>
                    </tr>
                <% }); %>
            </tbody>
        </table>
    </div>
<%- include('../../partials/footer') %> 
    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
